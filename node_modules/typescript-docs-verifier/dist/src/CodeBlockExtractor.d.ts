export declare class CodeBlockExtractor {
    static readonly TYPESCRIPT_CODE_PATTERN: RegExp;
    private constructor();
    static extract(markdownFilePath: string): Promise<{
        code: string;
        type: "tsx" | "ts";
    }[]>;
    private static readFile;
    private static extractCodeBlocksFromMarkdown;
}
